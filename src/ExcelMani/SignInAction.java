package ExcelMani;

import org.openqa.selenium.WebDriver;

/*public class SignInAction {
// Now this method does not need any arguments
public static void Execute(WebDriver driver) throws Exception
{
	//This is to get the values from Excel sheet, passing parameters (Row num &amp; Col num)to getCellData method
	String sUsername = ExcelUnit.getCellData(1, 1);
	String sPassword = ExcelUnit.getCellData(1, 2);
	Home_Page.lnk_MyAccount(driver).click();
	Login_Page.txtbt_UserName(driver).sendKeys(sUsername);
	Login_Page.txtbt_Password(driver).sendKeys(sPassword);
	Login_Page.btn_Login(driver).click();
	}
}*/
